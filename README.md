# Skyward Analytics — Starter Website

This is a starter Next.js (App Router) site scaffold with Tailwind CSS, Docker, and GitHub Actions CI.

## Quick start

1. `npm ci`
2. `npm run dev`
3. Open http://localhost:3000

## Deployment

- Vercel: import the repo and deploy.
- Docker: `docker build -t skyward-website . && docker run -p 3000:3000 skyward-website`.

